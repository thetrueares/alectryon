services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    networks:
      - app-network

  web:
    build: ./web
    ports:
      - "5173:5173"
    depends_on:
      - api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
